# Rustic (Rust crate)

Project documentation, Docker build, and security model: **[`README.md` in the repository root](../README.md)**.

This directory is the **`rustic`** Cargo package — run `cargo` / `rustup` from here.
